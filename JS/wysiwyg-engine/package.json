{
  "name": "@anglesite/wysiwyg-engine",
  "version": "0.1.0",
  "private": true,
  "description": "Portable, dependency-free TypeScript engine for the WYSIWYG block editor (spec: docs/superpowers/specs/2026-08-03-modern-wysiwyg-editor-design.md), plus the always-on page bridge (Siri visible-elements reporter, pick modes, Component Editor harness canvas). The only script injected into the preview since #1957.",
  "type": "module",
  "scripts": {
    "build": "esbuild src/host/mount.ts --bundle --format=iife --target=es2022 --outfile=../../Resources/wysiwyg-engine/engine.js",
    "typecheck": "tsc --noEmit",
    "lint": "oxlint src test e2e",
    "test": "vitest run",
    "test:watch": "vitest",
    "build:e2e": "esbuild e2e/fixture-page.ts --bundle --format=iife --target=es2022 --outfile=e2e/.generated/fixture-bundle.js && esbuild e2e/breakpoints-fixture-page.ts --bundle --format=iife --target=es2022 --outfile=e2e/.generated/breakpoints-bundle.js",
    "test:e2e": "npm run build:e2e && playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "@types/node": "^26.4.1",
    "esbuild": "^0.28.2",
    "jsdom": "^30.0.1",
    "oxlint": "^1.81.0",
    "typescript": "^7.0.2",
    "vitest": "^5.0.0"
  },
  "engines": {
    "node": ">=22"
  }
}
