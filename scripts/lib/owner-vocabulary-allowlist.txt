# Reviewed exceptions for scripts/check-localization-catalog.sh's owner-vocabulary lint
# (#1963, decision D1). One exact Sources/AnglesiteApp/Localizable.xcstrings key per line;
# `#` comments and blank lines are ignored. Every entry needs a reason and, where the
# exception is temporary, the issue that retires it. Keys whose only call sites are the
# Debug pane (DebugPaneView*.swift) are exempt automatically and don't belong here.

# Settings ▸ Advanced ▸ Safari MCP Bridge / LAN runtime: developer-facing sections that
# #1964 gates behind the Advanced developer-tools toggle. "MCP" is the protocol's own name;
# an owner who reaches these controls has opted into the technical surface.
Safari MCP Bridge
Safari MCP bridge port
Safari MCP runs locally on this Mac. Once bridged, it can expose the active Safari tab's page content, screenshots, and console logs to whatever is listening on that port — only run the bridge command when you trust what's on the other end.
MCP port
LAN runtime MCP port

# SyncConflictResolutionSheetView is rewritten wholesale by #1964 (D1: phrased in terms of
# the owner's content, not files and Macs). Drop this entry when that lands.
This copy's content already exists in this site's git history
